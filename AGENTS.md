# Repository Notes

- For browser-based verification, first try the existing local dev server at `http://localhost:4321`.
- Use browser tools against that running server before starting a new dev server or preview server.
- If `localhost:4321` is unavailable, then fall back to starting the appropriate local server yourself.
